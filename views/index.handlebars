
<div class="wrapper">
	{{#each data.user}}
		<p>Welcome {{user_name}}</p>
		{{/each}}
	<a href="/user">View Your Profile</a>
	</div>
	<h1>Groups</h1>
	<div class="group-div">
		{{#each data.groups}}
			<li>
				<a href="/details/{{id}}">{{group_name}}</a> <br>
				{{group_desc}} <br>
				{{!-- <button id="join-button" data-userId="{{id}}" data-groupId="{{id}}" type="submit">Join group</button> --}}
			</li>
		{{/each}}
	</div>
</div>

{{!-- 
    <form>
        <div class="loginTitle">
            <h2>Log In</h2></div>
        <div class="form-group">
            <label for="exampleInputEmail1">Email Address</label>
            <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email">
        </div>
        <div class="form-group">
            <label for="exampleInputPassword1">Password</label>
            <input type="password" class="form-control" id="exampleInputPassword1" placeholder="Password">
        </div>
        <div class="btnContainer">
            <button type="submit" class="btn btn-primary login">Login</button>
            <a href="/signup"><button type="submit" class="btn btn-primary signup ">Sign Up</button></a>
        </div>
        
    </form> --}}

